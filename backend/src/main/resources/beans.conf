include "udash.conf"

beans {
  uiServer = {
    %class = io.udash.guide.server.ApplicationServer
    %constructor-args = [
      ${ui.server.port},
      ${ui.server.homepageResourceBase},
      ${ui.server.guideResourceBase}
    ]
  }
}