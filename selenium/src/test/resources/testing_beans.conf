beans {
  webDriver = {
    %class = org.openqa.selenium.firefox.FirefoxDriver
  }

  // Start server for every test suite
  serverConfig = {
    %class = io.udash.guide.InternalServerConfig
  }

  // Use external server
  //serverConfig = {
  //  %class = io.udash.guide.ExternalServerConfig, %construct = true
  //  urlPrefix = "http://localhost:8888"
  //}
}